diff --git a/src/DLP.cpp b/src/DLP.cpp
index 7016598..2284d42 100644
--- a/src/DLP.cpp
+++ b/src/DLP.cpp
@@ -97,16 +97,24 @@ bool DLP::DLPCheck(CUpDownClient* c){
 			tmp = ret.c_str();
 		}
 	}
-	
+
+	if (c->HasLowID() && (tmp == NULL)) {
+		ret = _("Ban Low ID");
+		tmp = ret.c_str();
+	}
+
 	if (tmp != NULL) {
 		ret = tmp;
 		wxString wxInfo;
-		wxInfo.Printf(wxT("[%s] %s"), ret.c_str(), c->GetClientFullInfo().c_str());
+		wxInfo.Printf(wxT("[%s][%s][%s]"), uhash.c_str(), ret.c_str(), c->GetClientFullInfo().c_str());
 		c->Ban();
 		theApp->AddDLPMessageLine(wxInfo);
 		return true;
 	}
 
+	wxString wxInfo;
+	wxInfo.Printf(wxT("[%s][Record IP][%s]"), uhash.c_str(), c->GetClientFullInfo().c_str());
+	theApp->AddDLPMessageLine(wxInfo);
 	return false;
 
 }
